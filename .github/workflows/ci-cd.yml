name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  release:
    types: [ created ]

jobs:
  # test:
  #   name: Run Tests
  #   runs-on: ubuntu-latest
    
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'

  #     - name: Install dependencies
  #       run: npm ci

  #     - name: Run linter
  #       run: npm run lint

  #     - name: Run unit tests
  #       run: npm test -- tests/unit.test.ts

  #     - name: Build project
  #       run: npm run build

  # integration-test:
  #   name: Run Integration Tests
  #   runs-on: ubuntu-latest
  #   needs: test
    
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'

  #     - name: Install dependencies
  #       run: npm ci

  #     - name: Run integration tests
  #       run: npm test -- tests/integration.test.ts
  #       env:
  #         GHION_API_KEY: ${{ secrets.GHION_API_KEY }}
  #         GHION_API_SECRET: ${{ secrets.GHION_API_SECRET }}
  #         GHION_API_PASSPHRASE: ${{ secrets.GHION_API_PASSPHRASE }}

  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    needs: [test, integration-test]
    if: github.event_name == 'release' && github.event.action == 'created'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Publish to npm
        run: npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
